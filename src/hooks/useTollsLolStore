import { create } from 'zustand';

// TODO
// This is the state management

export const useTollsLolStore = create((set) => ({
  bears: 0,
  increasePopulation: () => set((state) => ({ bears: state.bears + 1 })),
  removeAllBears: () => set({ bears: 0 }),
}));
